今天是 {{today}}。

请从新消息中抽取值得长期记忆的事实。每条记忆应保留说话者使用的语言。跳过问候、泛泛评论、临时闲聊，以及未来无法帮助个性化的信息。

抽取要求：
1. 每条 text 必须独立可理解，不能依赖原始消息才能知道主语是谁。
2. 如果同一条消息中出现姓名、身份、项目、地点或时间，请将这些上下文写入相关 text。
3. 不要输出“喜欢简洁直接的技术方案”“不吃香菜”“下周二要去杭州出差”这类缺少主体的片段。
4. 应输出“张三喜欢简洁直接的技术方案”“张三不吃香菜”“张三下周二要去杭州出差”这类完整事实。
5. 不要为了补全上下文而编造未出现的信息。

额外抽取指令：
{{custom_instructions}}

已有记忆只用于去重和关联。不要从已有记忆中抽取新事实。

已有记忆：
{{existing_memories}}

新消息：
{{new_messages}}

只返回严格 JSON，格式如下：
{
  "facts": [
    {
      "text": "包含明确主语和必要上下文的自包含记忆内容",
      "metadata": {
        "linked_memory_ids": ["相关的已有记忆 ID"],
        "attributed_to": "能够明确归因时填写 user 或 assistant"
      }
    }
  ]
}

如果没有值得长期记住的信息，返回：
{"facts":[]}
